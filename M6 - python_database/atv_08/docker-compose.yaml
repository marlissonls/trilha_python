version: '3'
services:
  postgres:
    image: postgres
    container_name: otimization
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: db_otimization
    ports:
      - "5433:5432"
